export interface Campaign {
    id: string;
    membership: string;					
    store_name: string;					
    event_imgUrl: string;				
    campaign_title: string;			
    campaign_description: string;	
    campaign_elements: Array<string>;
}
